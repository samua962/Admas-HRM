<UserControl x:Class="Admas_HRM2.Department_Detail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <DropShadowEffect x:Key="CardShadow" Color="#40000000" BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#2C3E50" Offset="0"/>
            <GradientStop Color="#3498DB" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="TextBlock" x:Key="SectionTitle">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0 0 0 16"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="DetailLabel">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="Margin" Value="0 0 0 4"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="DetailValue">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="0 0 0 12"/>
        </Style>

        <!-- Button Style -->
        <Style x:Key="MyButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2980B9"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#bee6fd"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Border Background="White" 
                CornerRadius="8" 
                Effect="{StaticResource CardShadow}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="24">
                    <!-- Header -->
                    <Border Background="{StaticResource HeaderGradient}"
                            CornerRadius="6"
                            Padding="24"
                            Margin="0 0 0 24">
                        <StackPanel>
                            <TextBlock Text="DEPARTMENT Detail"
                                     FontSize="20"
                                     FontWeight="Bold"
                                     Foreground="White"/>
                            <TextBlock x:Name="txtDepartmentName"
                                     FontSize="24"
                                     FontWeight="SemiBold"
                                     Foreground="White"
                                     Margin="0 4 0 0"/>
                        </StackPanel>
                    </Border>

                    <!-- Key Metrics -->
                    <UniformGrid Columns="3" Margin="0 0 0 24">
                        <Border Background="#F8F9FA" CornerRadius="6" Padding="16">
                            <StackPanel>
                                <TextBlock Text="Department Head" Style="{StaticResource DetailLabel}"/>
                                <TextBlock x:Name="txtManager" Style="{StaticResource DetailValue}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#F8F9FA" CornerRadius="6" Padding="16" Margin="12 0">
                            <StackPanel>
                                <TextBlock Text="Status" Style="{StaticResource DetailLabel}"/>
                                <TextBlock x:Name="txtStatus" Style="{StaticResource DetailValue}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#F8F9FA" CornerRadius="6" Padding="16">
                            <StackPanel>
                                <TextBlock Text="Established" Style="{StaticResource DetailLabel}"/>
                                <TextBlock x:Name="txtCreatedDate" Style="{StaticResource DetailValue}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                    <!-- Description Section -->
                    <Border Background="#F8F9FA" CornerRadius="6" Padding="16" Margin="0 0 0 24">
                        <StackPanel>
                            <TextBlock Text="Description" Style="{StaticResource SectionTitle}"/>
                            <TextBlock x:Name="txtDepartmentDescription"
                                     Style="{StaticResource DetailValue}"
                                     TextWrapping="Wrap"
                                     LineHeight="24"/>
                        </StackPanel>
                    </Border>

                    <!-- Action Bar -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource MyButtonStyle}"
                                Width="120"
                                Height="40"
                              Content="Close"
                              Click="btnClose_Click"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>