<UserControl x:Class="Admas_HRM2.Edit_Department"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600"
             Background="White">

    <UserControl.Resources>
        <!-- Button Style -->
        <Style x:Key="MyButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2980B9"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#bee6fd"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="5">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton"
                                  Grid.Column="2"
                                  Focusable="False"
                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                  ClickMode="Press">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent"
                                        BorderThickness="0"
                                        Padding="10">
                                                <Path x:Name="Arrow"
                                          Fill="{TemplateBinding Foreground}"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          Data="M0,0 L4,4 8,0 Z"/>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"
                                      Margin="{TemplateBinding Padding}"
                                      IsHitTestVisible="False"
                                      Content="{TemplateBinding SelectionBoxItem}"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                                <TextBox x:Name="PART_EditableTextBox"
                             Style="{x:Null}"
                             HorizontalAlignment="Left"
                             VerticalAlignment="Center"
                             Margin="{TemplateBinding Padding}"
                             Focusable="True"
                             Background="Transparent"
                             Visibility="Hidden"
                             IsReadOnly="{TemplateBinding IsReadOnly}"/>
                                <Popup x:Name="Popup"
                           Placement="Bottom"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           AllowsTransparency="True"
                           Focusable="False"
                           PopupAnimation="Slide">
                                    <Border Background="White"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                        Padding="10,5">
                                        <ScrollViewer Margin="1">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="describsion" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                     BorderBrush="{TemplateBinding BorderBrush}" 
                     BorderThickness="{TemplateBinding BorderThickness}" 
                     CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#2980b9" Offset="0"/>
            <GradientStop Color="#6dd5fa" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">

        <StackPanel>

            <Border Background="{StaticResource HeaderGradient}" CornerRadius="5" Padding="15" Margin="20">
                <TextBlock Text="Manage Department" FontSize="26" FontWeight="Bold" Foreground="White"
           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <Grid Background="White" Margin="100,40,100,50">
                <!-- Grid and Row Definitions -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Title -->
                    <RowDefinition Height="Auto"/>
                    <!-- Department ID -->
                    <RowDefinition Height="Auto"/>
                    <!-- Department Name -->
                    <RowDefinition Height="Auto"/>
                    <!-- Department Manager -->
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <!-- Department Description -->
                    <RowDefinition Height="Auto"/>
                    <!-- Buttons -->
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Text="Edit Department" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"
                       Grid.Row="0" Foreground="Black" Margin="0,0,0,20"/>

                <!-- Department ID -->
                <StackPanel Grid.Row="1" Margin="0,0,0,10">
                    <TextBlock Text="Department ID:" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtDepartmentID" Height="40" Padding="10"
                         BorderThickness="1" Background="#F0F0F0" FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Department Name -->
                <StackPanel Grid.Row="2" Margin="0,0,0,10">
                    <TextBlock Text="Department Name:" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtDepartmentName" Height="40" Padding="10"
                         BorderThickness="1" Background="#F0F0F0" FontSize="14"/>
                </StackPanel>

                <!-- Department Manager -->
                <StackPanel Grid.Row="3" Margin="0,0,0,10">
                    <TextBlock Text="Department Manager:" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtDepartmentHead" Height="40" Padding="10"
                         BorderThickness="1" Background="#F0F0F0" FontSize="14"/>
                </StackPanel>

                <!-- Department Status -->
                <StackPanel Grid.Row="4" Margin="0,0,0,10">
                    <TextBlock Text="Department Status:" VerticalAlignment="Center" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cbDepartmentStatus" Height="40" Padding="10"
              BorderThickness="1" Background="#F0F0F0" FontSize="14">
                        <ComboBoxItem Content="Active" />
                        <ComboBoxItem Content="Inactive" />
                    </ComboBox>
                </StackPanel>

                <!-- Department Description -->
                <StackPanel Grid.Row="5" Margin="0,0,0,10">
                    <TextBlock Text="Department Description:" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtDepartmentDescription" Height="80" Padding="15"
                         Style="{StaticResource describsion}" BorderThickness="1" Background="#F0F0F0" FontSize="14" AcceptsReturn="True"/>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="6" Margin="0,20,0,0">
                    <Button x:Name="btnSubmit" Content="Update" Cursor="Hand" Width="120" Height="40" Margin="10" Padding="10,5" Click="UpdateDepartment_Click" Style="{StaticResource MyButtonStyle}"/>
                    <Button x:Name="btnCancel" Content="Cancel" Background="Gray" Cursor="Hand" Width="120" Height="40" Margin="10" Padding="10,5" Click="btnCancel_Click" Style="{StaticResource MyButtonStyle}"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
